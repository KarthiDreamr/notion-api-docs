[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/notion/v1/*"
  to = "/.netlify/functions/notion-proxy/:splat"
  status = 200
  force = true 